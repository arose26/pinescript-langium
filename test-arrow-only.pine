// Test file for arrow functions only

// Simple arrow function with expression
simpleArrow(a, b) => a + b

// Arrow function with default parameters
defaultParams(a, b=10) => a + b

// Test usage
var result1 = simpleArrow(5, 10)
var result2 = defaultParams(5)
