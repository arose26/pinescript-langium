//@version=5
indicator("Multi-Statement Function Test")

// Function with multiple statements
calculate(a, b) =>
    sum = a + b
    product = a * b
    result = sum + product
    result

// Call the function
value = calculate(3, 4)

// Plot the result
plot(value, "Result", color=color.blue)
