//@version=5
indicator("Logical OR Test")

// Basic variables
a = true
b = false

// Logical OR operator
result = a or b  // true

// Plot the result (convert boolean to number for plotting)
plot(result ? 1 : 0, "Result", color=color.green)
