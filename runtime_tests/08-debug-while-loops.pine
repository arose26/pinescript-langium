//@version=5
indicator("Debug While Loops Test")

// Simple while loop with break
counter = 0
sum = 0
while counter < 10
    sum := sum + counter
    counter := counter + 1
    if counter >= 5
        break

// Plot the results
plot(sum, "Sum 0-4", color=color.blue)
