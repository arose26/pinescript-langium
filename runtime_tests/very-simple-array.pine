//@version=5
indicator("Very Simple Array Test")

// Create a function that returns a simple value
create_value() =>
    5

// Call the function and plot the result
total_value = create_value()

// Plot the value
plot(total_value, "Value", color=color.blue)
