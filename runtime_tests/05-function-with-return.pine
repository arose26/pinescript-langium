//@version=5
indicator("Function with Return Test")

// Function with explicit return
calculate(a, b) =>
    sum = a + b
    product = a * b
    return sum + product

// Call the function
value = calculate(3, 4)

// Plot the result
plot(value, "Result", color=color.blue)
