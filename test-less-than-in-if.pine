//@version=5
indicator("Less Than in If Test")

// Test less than operator in an if statement
var a = 5
var b = 10

if a < b
    var result = "a is less than b"
else
    var result = "a is not less than b"

// Plot the result
plot(1, "Result", color=color.blue)
