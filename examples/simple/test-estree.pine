// Simple test script for ESTree conversion
var a = 1
var b = 2
var c = a + b

if c > 2
    c := c + 1
else
    c := c - 1

// Function definition
f(x) =>
    y = x * 2
    y + 1

// Using the function
var result = f(c)

// Array operations
var arr = [1, 2, 3]
arr.push_back(4)
var last = arr.pop_back()

// Loop
var sum = 0
for i = 0 to 10
    sum := sum + i

// While loop
var counter = 0
while counter != 5
    counter := counter + 1
