// Testing advanced built-in functions
var wma = ta.wma(close, 14)
var vwma = ta.vwma(close, 14)
var macd = ta.macd(close, 12, 26, 9)
var bb = ta.bbands(close, 20, 2.0)
var priceStr = str.tostring(close, "#.##")
var formattedStr = str.format("O: {0}, H: {1}, L: {2}, C: {3}", open, high, low, close)
var strLen = str.length("Hello")
var subStr = str.substring("Hello", 1, 3)
var floatArray = array.new_float(5, 0.0)
var intArray = array.new_int(5, 0)
var strArray = array.new_string(5, "")
array.set(floatArray, 0, close)
array.set(intArray, 0, 42)
array.set(strArray, 0, "Hello")
var firstFloat = array.get(floatArray, 0)
var arraySize = array.size(floatArray)
array.push(floatArray, high)
var lastValue = array.pop(floatArray)
array.clear(strArray)
