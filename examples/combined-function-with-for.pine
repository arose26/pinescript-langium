//@version=5
indicator("Combined Function with For Loop", overlay=false)

// Function with for loop
f() =>
    sum = 0
    for i = 1 to 5
        sum := sum + i
    sum

// Use the function
result = f()

// Plot the result
plot(result, "Result", color=color.blue)
