//@version=5
indicator("Template and Less Than Test")

// Define a function with a template specification
myFunc<T>(a, b) =>
    a + b

// Test less than operator
var a = 5
var b = 10
var isLessThan = a < b

// Plot the result
plot(1, "Result", color=color.blue)
