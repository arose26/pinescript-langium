//@version=5
indicator("Inequality Test Workaround")

// Simple inequality test using workaround
value = 5

// Use greater than instead of less than
if 10 > value
    result = "less than 10"
else
    result = "not less than 10"

plot(1, "Result: " + result, color=color.blue)
